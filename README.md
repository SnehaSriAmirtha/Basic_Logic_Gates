# Basic_Logic_Gates
Design and implement all basic logic gates (AND, OR, NOT, NAND, NOR, XOR, XNOR) using Verilog. Each gate should be implemented as a separate module with proper input/output declarations.
